<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Required for Tauri's bundled sidecar + Ollama child process to spawn
         after we enable hardened runtime. Without this the notarized .app
         refuses to exec its own embedded binaries. -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>

    <!-- IMSCC files are user-selected; the sandbox file-access only matters
         if we turn on sandboxing, but leave these enabled so the Ollama
         models dir and IMSCC drops survive if a future version sandboxes. -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
    <key>com.apple.security.files.downloads.read-write</key>
    <true/>

    <!-- Bundled Ollama + LiteParse both open localhost listeners. -->
    <key>com.apple.security.network.client</key>
    <true/>
    <key>com.apple.security.network.server</key>
    <true/>
</dict>
</plist>
